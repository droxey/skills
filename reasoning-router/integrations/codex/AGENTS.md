# Global Reasoning Selection

Before every task:

1. Load and follow the `reasoning-router` skill.
2. Select the lowest sufficient available reasoning mode.
3. Respect an explicit user-selected mode.
4. Never claim to activate a mode that the current surface does not expose.
5. When the harness cannot switch modes, continue with the current mode and state the recommended mode only when the difference is material.
